<Application x:Class="Interactive_Map.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <Style x:Key="TruncatedTextBlockStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="FontWeight" Value="DemiBold" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="TextTrimming" Value="WordEllipsis" />
            <Setter Property="Width" Value="180" />
        </Style>

        <Style x:Key="ImageBorderStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="5" />
            <Setter Property="BorderBrush" Value="LightGray" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style x:Key="GameListItemContainerStyle" TargetType="ContentPresenter">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Margin" Value="10" />
            <Setter Property="Focusable" Value="False"/>
        </Style>

        <Style x:Key="GameListStyle" TargetType="ItemsControl">
            <Setter Property="Padding" Value="10" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="ItemContainerStyle" Value="{StaticResource GameListItemContainerStyle}" />
        </Style>
    </Application.Resources>
</Application>
